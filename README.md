# Smart Invoice Builder — Truffle & Scent Static Release

This release is a static GitHub Pages version of the invoice app. It does not need npm, Vite, Node, or a build step.

Expected live sender block:

Truffle & Scent By Lee
DTI BN No. 8301368
TIN: 807-789-743-000
Pembo
Taguig Manila, Philippines
+63956 533 1521

The root `index.html` loads the already-built files in `assets/`. The included GitHub Actions workflow deploys the static files directly to GitHub Pages without running `npm install`.

For a clean repository upload, extract this zip and upload the contents of this folder directly into the repository root.
